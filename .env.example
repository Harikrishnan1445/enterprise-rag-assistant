# App
APP_NAME=Enterprise Knowledge Intelligence & RAG Assistant
ENVIRONMENT=development

# Database (we'll use real values once Postgres is running in Phase 7)
DATABASE_URL=postgresql+asyncpg://raguser:ragpassword@localhost:5432/ragdb

# JWT (we'll generate a real secret in Phase 11)
JWT_SECRET_KEY=changeme-generate-a-real-secret-later
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30